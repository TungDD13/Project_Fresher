@model IEnumerable<FA.JustBlog.Core.Models.Tag>
@using FA.JustBlog.Core.Models;

@{
    ViewBag.Title = "Index";
}

<h2>@Html.ActionLink("Create", "Create")</h2>
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">All Tag</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example1" class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>UrlSlug</th>
                                <th>Description</th>
                                <th>Count</th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Tag item in Model)
                            {
                                <tr>
                                    <td>@item.Name</td>
                                    <td>@item.UrlSlug</td>
                                    <td>@item.Description</td>
                                    <td>@item.Count</td>
                                    <td>@Html.ActionLink("Edit", "Edit", new { name = item.Name })</td>
                                    <td>@Html.ActionLink("Details", "Details", new { name = item.Name })</td>
                                    @if (@User.IsInRole("BlogOnwer"))
                                    {
                                        <td>@Html.ActionLink("Delete", "Delete", new { name = item.Name })</td>
                                    }

                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            @*<tr>
                                    <th>Rendering engine</th>
                                    <th>Browser</th>
                                    <th>Platform(s)</th>
                                    <th>Engine version</th>
                                    <th>CSS grade</th>
                                </tr>*@
                        </tfoot>
                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
<!-- /.content -->
